//! The texts a session reads before it calls anything: generated from the Java text blocks, with
//! the text-block rules applied (incidental indentation, `\` line joins, trailing spaces), so a
//! description is character for character the one the Java server sent.

/// What `server/discover` hands a client: the whole server in one paragraph.
pub const INSTRUCTIONS: &str = "Rekall holds one user's companies, projects, tasks and markdown notes, and hands back a whole working context in a single call. Anchor what you need as `entity:value`, for example `project:vega task:report-builder`. Reading is `rekall_context`. The writes are few and narrow: a task's wrapup (`rekall_wrapup`, what its implementation looks like now, replaced in place), a step's progress up to `claimed` (`rekall_step`), a commit logged against a task (`rekall_record_commit`), a proposed step that lands as a draft for a person to promote (`rekall_propose_step`), and a new note on one task (`rekall_note`), which only adds. Nothing else here can be changed.";

/// `rekall_record_commit`.
pub const RECORD_COMMIT: &str = "Log the commit you just made against this task, or one of its steps. Call it right after `git commit`, once, with nothing but the anchor: it reads the tip of the project's own repo folder itself, so there is no hash or message to pass.\n\n`commit` is optional: the hash of an earlier commit, abbreviated or full, when the one to log is not the tip any more. Leave it out for the commit you just made.\n\nAnchor the task the way `rekall_context` does: `project:vega task:report-builder`. It has to name exactly one task.\n\n`step` is optional: the step's one-based position in the checklist (`\"3\"`) or its exact title. Leave it out to log the commit against the task itself.\n\nThe comment stored is the commit's own subject line, so write that commit message as the one line you would want to see next to this task later. Logging the same commit against the same task and step twice is a no-op, not a duplicate.\n";

/// `rekall_context`.
pub const CONTEXT: &str = "Load the full working context for one or more anchors in a single call.\n\nAn anchor is `entity:value`, for example `project:vega task:report-builder`. The\nentities are `company`, `project` and `task`.\n\nThe value is the record's label, not its title. A label is lowercase, has no\nspaces, and is unique inside its parent: `project:vega`, never\n`project:\"Vega Platform\"`. The title is what the record is called and is free to\nchange; the label is what this tool resolves.\n\nA bare term with no `entity:` is looked up across all three and accepted only when\nexactly one record matches; otherwise the candidates come back and nothing loads.\n\nA note can be attached to several tasks, so the same markdown may arrive under\nmore than one anchor. That is deliberate: it is written once and read wherever\nit applies.\n\nA note marked `loaded=\"on request\"` arrives as its title, its first line and an\nanchor such as `note:3f2a9c1e`, not its body. Its person judged most sessions do\nnot need it. If the work does, call this tool with that anchor and the note comes\nback in full; if it does not, leave it.\n\nA task also carries its wrapup, if it has one: what its implementation looks\nlike now, written at the end of the last session. Read it as the current state of\nthe work, not as a history of it. Replacing it is `rekall_wrapup`.\n\nA task may also carry a checklist of steps, and it is what says where the work\nhas got to. An open step is the work: it arrives with its detail and it is what\nthe session is for. A done step arrives as its title alone, because it is\nfinished: do not build it again, and do not treat the missing detail as\nsomething to go and find. Read the open ones as the list of what is left, in\norder, and if the checklist and the wrapup disagree, the checklist is the one a\nperson ticked.\n\nA done step marked `(finished since the wrapup was written)` is work the wrapup\npredates and cannot mention. It arrives with its detail for that reason alone,\nand it is what the next `rekall_wrapup` has to fold in: nothing else here says\nwhat that piece was.\n\nA draft step is not in the checklist you get. It is a line the person is still\nwording, counted only as `draft=\"N\"` on the `<steps>` tag. It carries no work\nyet, `rekall_step` will not move it, and it becomes a real step when the console\npromotes it.\n\nWhere a task has open steps, they are the work and its description is not. The\ndescription is the standing context to build a step against: what the task is\nfor, what the work has to satisfy, what is out of scope. It is written once and\ndoes not shrink as the work is done, so it goes on naming things that are\nalready built, and reading it as a list of instructions is how the same thing\ngets built twice.\n\nNothing you can call changes a step. They are ticked by hand in the console, by\nthe person who reviewed the work.\n\nA task may also hand over commits. Each `<commit>` inside `<commits>` is a change\nthe person chose in the console to travel with this context: its hash, its subject,\nthe step it was logged against if any, and the diff it introduced. Read a diff as\nthe change it records, not as the current state of the file: it is where to start\nlooking, and the repository is what to trust. A task with no `<commits>` block has\nnothing chosen; `rekall_record_commit` logs a commit but does not choose it.\n\nEach anchor returns the record, what it references resolved in full with their\nnotes, what references it as anchors you can pass back, and all of its markdown.\n";

/// `rekall_note`.
pub const NOTE: &str = "Write a new markdown note and attach it to one task. It loads with that task's context from then on, in full, and a person can edit it, share it with other tasks or delete it in the console.\n\nUse it when the work produces something worth keeping that is not the state of the implementation: the output a step or the description asks for (an analysis, a list, a draft, a runbook), a reference someone will want again, or a note the task exists to write. It is not the wrapup: what the implementation looks like now goes to `rekall_wrapup`, and a note does not replace it.\n\n- `title`: what the note is, short, the way a file would be named (\"export-formats.md\").\n- `body`: the whole note, in markdown, written to be read on its own by someone who was not in this session.\n\nIt only adds. A title the task's notes already carry is refused rather than overwritten, and nothing here can change a note that is already there.\n\nAnchor the task the way `rekall_context` does: `project:vega task:report-builder`.\n";

/// `rekall_propose_step`.
pub const PROPOSE_STEP: &str = "Propose one step for a task's checklist. It lands as a draft on the staging shelf in the console, never on the checklist itself: a person reads it, rewords it if they want, and promotes it or deletes it. Until then no session sees it as work and `rekall_step` will not move it.\n\nUse it when you were asked to plan a task. Read the task with `rekall_context` first: the description says what the work is for and what it has to satisfy, the wrapup says what already exists, and the code says the rest. Propose only what is still missing, one call per step, in the order the work should be done.\n\n- `title`: what the step delivers, as a short imperative line (\"Expose the report as a download\"), under 200 characters.\n- `detail`: markdown. What to build, where it goes in the code, what it must satisfy, and how a reviewer can tell it is done. Enough for a session that never saw this one to do the step alone.\n\nA title the task already has is refused, so running a plan again does not duplicate it. A task holds at most 20 drafts at once.\n\nAnchor the task the way `rekall_context` does: `project:vega task:report-builder`.\n";

/// The answer to a proposal: title, draft count, plural suffix.
pub const PROPOSED_REPORT: &str = "Draft \"%s\" proposed. The task holds %d draft%s waiting on the console's staging shelf; none of them is work until a person promotes it.";

/// `rekall_step`.
pub const STEP: &str = "Move one step of a task along its line: `open` -> `running` -> `claimed`.\n\nUse it to drive a checklist. Mark the step you are about to work `running`, do the work, write the wrapup, then mark it `claimed`. If a later step is still open, mark that one `running` and go again.\n\nAnchor the task the way `rekall_context` does: `project:vega task:report-builder`. It has to name exactly one task.\n\n`step` is the step's one-based position in the checklist, as `rekall_context` lists them (`\"3\"`), or its exact title.\n\n`state` is one of:\n- `running`  a session is working on this step now\n- `claimed`  a session has finished it and it is waiting for the console to accept it\n- `open`     put a step back, for when a session abandons it\n\nThere is no `done` here. A step is ticked done in the console, by the person who reviewed the work. Ask for `claimed` when you are finished and leave the last move to them.\n\nOn a project set to auto-commit, `claimed` also commits everything in the project's folder and logs that commit against the step; the answer says so. Do not commit by hand on such a project. Pass `commit_message` with the claim so the commit says what the step built: without it, the message is drawn from the step's title and detail.\n";

/// `rekall_wrapup`.
pub const WRAPUP: &str = "Record what a task's implementation looks like now. One task, one wrapup, replaced in place.\n\nAnchor the task the way `rekall_context` does: `project:vega task:report-builder`. A bare `task:<label>` works when that label exists on only one project.\n\n`body` is the complete new text and it overwrites whatever was there. Nothing is merged, so send the wrapup in full every time, not the part that changed. The version it replaces goes to the wrapup's history in the console, where a person can restore it; no session reads that history.\n\nWrite the state, never the story of getting there. It describes the system as it stands, for a reader who was not in this session and does not care what it looked like before:\n\n- Yes: what exists, what it does, how the parts fit, what is settled and what is still open, where the sharp edges are.\n- No: \"added\", \"changed\", \"now also\", \"previously\", \"fixed\", \"refactored\", \"before/after\", anything dated, anything phrased as a step you took.\n\nIf a sentence only makes sense to someone who watched the change happen, it does not belong in a wrapup. Notes are where the reasoning and the history go.\n\nName the code, do not transcribe it. It is read next to the repository, so say where things are by the names they have: the class, the file, the endpoint, the table, the component, a line or two each on what it is for and what it decides.\n\n- Yes: what an entity is there for, and, where there is business logic, the rule itself: what it decides, on what, what happens at the edges, what is refused.\n- No: the fields of an object, the columns of a table, method signatures, parameter lists, a directory tree. That is in the code, it is longer than the wrapup, and it is wrong a week later.\n\nSmall enough to read in one go. Short paragraphs or short bullets, not an essay and not an index.\n\nRead the current wrapup with `rekall_context` before replacing it: it arrives with the task. Keep what is still true and rewrite the rest, rather than describing only the piece you touched.\n\nWritten after a step was finished, it has to fold that step's work into the same description rather than append to it. `rekall_context` marks every finished step this wrapup predates, and all of them belong in the new text, not only the one this session closed: a step ticked in an earlier session that never got a wrapup is still waiting to be described. One account of the whole task, with the new piece named where it lives; never a section per step, and never a heading carrying a step's title. If the step made something the wrapup already said untrue, that sentence goes.\n\nOn a task with no checklist, this wrapup is what claims the work. If its project is set to auto-commit, writing it also commits everything in the project's folder and logs that commit against the task; the answer says so. Do not commit by hand on such a project. Pass `commit_message` so the commit says what the work did: without it, the message is drawn from the task's title and this wrapup.\n";
